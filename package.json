{
  "name": "package",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/gestion_api.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:api": "json-server --watch db.json --port 3000",
    "dev:all": "concurrently \"npm run dev\" \"npm run dev:api\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "json-server": "^1.0.0-beta.3",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "axios": "^1.10.0"
  }
}
